import { RegisterForm } from "./register_form";

const AdminRegisterPage: React.Page = () => {
  return (
    <main className="flex h-[800px] w-full items-center justify-center">
      <RegisterForm />
    </main>
  );
};

export default AdminRegisterPage;
