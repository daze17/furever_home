import { ForgotPasswordForm } from "./forgot_password_form";

const ForgotPasswordPage: React.Page = () => {
  return (
    <main className="flex h-[calc(100dvh-190px)] w-full items-center justify-center">
      <ForgotPasswordForm />
    </main>
  );
};

export default ForgotPasswordPage;
