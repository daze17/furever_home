import { z } from 'zod';

export const setupZodErrorMap = () => {
  // z.setErrorMap((issue, ctx) => {
  //   console.log(issue, ctx);
  //   return {
  //     message: issue.message,
  //   };
  // });
};
